# Dependencies
node_modules/

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Environment & secrets
.env
.env.*
!.env.example
!.env.docker.example
!.env.prod.example

# Local dev bootstrap state (generated by scripts/dev-bootstrap.sh)
.dev-state
.dev-seed.json

# Nile testnet wallet (testnet key — gitignored even though it has no real funds)
scripts/.nile-wallet.json
.testnet-seed.json

# Superflow (local run state + internal product-discovery docs — not versioned)
.superflow-state.json
docs/superflow/
.worktrees
.worktrees/
.par-evidence.json

# Claude Code local state (contains local test harnesses with API keys — never commit)
.claude/
CLAUDE.local.md

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Coverage / test artifacts
coverage/

# Local audit artifacts (not for GitHub)
audits/

# OS
.DS_Store
Thumbs.db
.session-state.md
